- `buff_split()`

- `buff_vasprintf()`
- `t_buff *buff_format()`
- printf -> imprimer dans stdout
- dprintf -> imprimer dans n'importe quel fd
- fprintf -> imprimer dans n'importe quel FILE
- sprintf -> récupérer la string formattée
- snprintf -> récupérer la string formattée en se limitant à la taille disponible dans la string de destination
- asprintf -> je ne sais pas à quoi ça sert, explique moi - variantes v* -> accepte une valist au lieu des ...
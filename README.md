# libft project from 42 Paris school

## Criterion (unit test framework) install process

- macOS (Homebrew)
```sh
brew update
brew install criterion
```

- Ubuntu (apt)
```sh
sudo apt-get update
sudo apt-get install -y libcriterion-dev
```
